/*
    Currently we need english because all inventory parsers expects only english
    TODO: support other languages
 */
export const LANGUAGE = 'english';

export const STEAM_INVENTORY_API_PATH = 'inventory';
